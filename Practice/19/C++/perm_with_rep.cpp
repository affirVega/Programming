#include <iostream>

int main() {
	int length;
	std::string all_used_characters;
	std::cin >> length >> all_used_characters;

	/*
	 * Magic should happen here.
	 * In short: print all permutations with repetitions, such that:
	 *
	 *   - length >= all_used_characters.length()
	 *
	 *   - All characters from all_used_characters should be in permutation.
	 *	   So if length > all_used_characters.length(), then some characters
	 *	   may repeat.
	 *
	 * */
}
